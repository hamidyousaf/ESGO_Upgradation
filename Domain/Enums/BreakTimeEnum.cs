namespace Domain.Enums;

public enum BreakTimeEnum : byte
{
    ZeroMinute = 0,
    TenMinutes = 10,
    TwentyMinutes = 20,
    ThirtyMinutes = 30,
    FourtyMinutes = 40,
    FiftyMinutes = 50,
    SixtyMinutes = 60,
}
